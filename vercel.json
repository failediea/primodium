{
  "installCommand": "cd ../.. && pnpm install --ignore-scripts",
  "buildCommand": "cd ../.. && pnpm --filter client run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/drip", "destination": "/api/drip" }
  ]
}
