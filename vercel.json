{
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "pnpm --filter core build && cd packages/client && node --max-old-space-size=8192 node_modules/vite/bin/vite.js build --outDir ../../dist",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/drip", "destination": "/api/drip" }
  ]
}
