{
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "pnpm --filter core run build && node --max-old-space-size=8192 node_modules/vite/bin/vite.js build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/drip", "destination": "/api/drip" }
  ]
}
