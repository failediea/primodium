{
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "bash -c 'REPO_ROOT=$(pwd) && pnpm --filter core build && cd $REPO_ROOT/packages/client && node --max-old-space-size=8192 node_modules/vite/bin/vite.js build --outDir $REPO_ROOT/dist'",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/drip", "destination": "/api/drip" }
  ]
}
